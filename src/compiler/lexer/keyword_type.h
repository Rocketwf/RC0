#pragma once

enum class KeywordType {
    STRUCT,
    IF,
    ELSE,
    WHILE,
    FOR,
    CONTINUE,
    BREAK,
    RETURN,
    ASSERT,
    TRUE,
    FALSE,
    NULL_TYPE,
    PRINT,
    READ,
    ALLOC,
    ALLOC_ARRAY,
    INT,
    BOOL,
    VOID,
    CHAR,
    STRING
};